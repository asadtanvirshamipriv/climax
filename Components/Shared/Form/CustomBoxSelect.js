import { Select } from "antd";
import { Controller } from "react-hook-form";

const CustomBoxSelect = ({reset, useWatch, control, name, label}) => {

return(
    <>
    <div>{label}</div>
    <div className="dummy-input"></div>
    </>
)}

export default CustomBoxSelect